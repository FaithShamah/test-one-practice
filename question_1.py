#Question_1(a)
#Create a python program that calculates the area of the triangle area=1/2*b*h
base = float(input("Enter the base: "))
height = float(input("Enter the height: "))
Area = (1/2 * base * height)
print(f"The area of the triangle of base {base} and height {height} is {Area}")